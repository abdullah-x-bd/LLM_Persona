# Frozen prompts

All model-facing prompt templates for this experiment belong here.

Rules:

- prompts are versioned before paid runs
- target CAMS outcomes must never leak into persona context
- reasoning conditions must differ only in the intended reasoning control
- outputs should use a constrained machine-readable schema
- prompt version identifiers must be logged with every request
