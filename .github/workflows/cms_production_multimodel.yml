name: CMS historical multi-model workflow retired

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cms_production_multimodel.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cms-telecom-2025-multimodel-v1
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical inference workflow retired. Final study uses Luna and Claude only."
