name: CAMS historical cross-model workflow retired

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cams_crossmodel_robustness.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cams-crossmodel-robustness-v1
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical inference workflow retired. Final study uses Luna and Claude only."
