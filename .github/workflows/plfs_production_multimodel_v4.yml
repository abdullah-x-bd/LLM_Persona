name: PLFS historical multi-model workflow retired

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/plfs_production_multimodel_v4.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plfs-2023-24-production-v4
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical inference workflow retired. Final study uses Luna and Claude only."
