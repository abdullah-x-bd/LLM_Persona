name: CAMS Gemini diagnostic retired

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cams_gemini_failure_diag.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Gemini diagnostics are retired because Gemini is no longer part of the study."
